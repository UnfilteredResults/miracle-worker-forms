# Hindsight memory bank — travels with this repo via direnv.
# Read by: Codex CLI, OpenCode, Claude Code (via HINDSIGHT_BANK_ID).
# Requires: direnv installed and `direnv allow` run once in this repo.
export HINDSIGHT_BANK_ID="clt-miracle-worker-forms"
